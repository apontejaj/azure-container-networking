---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  labels:
    managed: ""
    owner: ""
  name: podnetworkinstances.multitenancy.acn.azure.com
spec:
  group: multitenancy.acn.azure.com
  names:
    kind: PodNetworkInstance
    listKind: PodNetworkInstanceList
    plural: podnetworkinstances
    shortNames:
    - pni
    singular: podnetworkinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PodNetworkInstance is the Schema for the PodNetworkInstances
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PodNetworkInstanceSpec defines the desired state of PodNetworkInstance
            properties:
              clusterNetworkConfig:
                description: ClusterNetworkConfig describes how to attach the infra
                  network to a Pod. optional for now in case orchestrator uses the
                  deprecated fields
                properties:
                  policyBasedRouting:
                    default: true
                    description: PolicyBasedRouting is a flag to enable policy based
                      routing
                    type: boolean
                  routes:
                    description: Routes is a list of routes to add to the Pod through
                      interface assigned to the infra network
                    items:
                      type: string
                    type: array
                    x-kubernetes-validations:
                    - message: DelegatedSubnet is not allowed in cluster network configuration
                        routes.
                      rule: '!(self.routes.all(x, toLower(x) == "delegatedsubnet"))'
                type: object
                x-kubernetes-validations:
                - message: Routes list shouldn't be empty if policybasedRouting is
                    disabled.
                  rule: self.policyBasedRouting || self.routes.size() > 0
              podIPReservationSize:
                default: 0
                description: Deprecated - use PodNetworks
                type: integer
              podNetworkConfigs:
                description: PodNetworkConfigs describes each PodNetwork to attach
                  to a single Pod optional for now in case orchestrator uses the deprecated
                  fields
                items:
                  description: PodNetworkConfig describes a template for how to attach
                    a PodNetwork to a Pod
                  properties:
                    podIPReservationSize:
                      default: 0
                      description: PodIPReservationSize is the number of IP address
                        to statically reserve
                      type: integer
                    podNetwork:
                      description: PodNetwork is the name of a PodNetwork resource
                      type: string
                    policyBasedRouting:
                      default: true
                      description: PolicyBasedRouting is a flag to enable policy based
                        routing
                      type: boolean
                    routes:
                      description: Routes is a list of routes to add to the Pod through
                        interface assigned to this PodNetwork
                      items:
                        type: string
                      type: array
                  required:
                  - podNetwork
                  type: object
                  x-kubernetes-validations:
                  - message: Routes list shouldn't be empty if policybasedRouting
                      is disabled.
                    rule: self.policyBasedRouting || self.routes.size() > 0
                type: array
              podnetwork:
                description: Deprecated - use PodNetworks
                type: string
            type: object
          status:
            description: PodNetworkInstanceStatus defines the observed state of PodNetworkInstance
            properties:
              networkStatuses:
                items:
                  description: PodNetworkStatus describes the status of a PodNetwork
                  properties:
                    network:
                      type: string
                    podIPAddresses:
                      items:
                        type: string
                      type: array
                    status:
                      description: PNIStatus indicates the status of PNI
                      enum:
                      - Ready
                      - CreateReservationSetError
                      - PodNetworkNotReady
                      - InsufficientIPAddressesOnSubnet
                      type: string
                  type: object
                type: array
              podIPAddresses:
                description: Deprecated - use PodNetworkConfigStatuses
                items:
                  type: string
                type: array
              podNetworkStatuses:
                additionalProperties:
                  description: PNIStatus indicates the status of PNI
                  enum:
                  - Ready
                  - CreateReservationSetError
                  - PodNetworkNotReady
                  - InsufficientIPAddressesOnSubnet
                  type: string
                description: Deprecated - use PodNetworkConfigStatuses
                type: object
              status:
                description: PNIStatus indicates the status of PNI
                enum:
                - Ready
                - CreateReservationSetError
                - PodNetworkNotReady
                - InsufficientIPAddressesOnSubnet
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: Only one default route is allowed across cluster and pod network
            configurations.
          rule: size(filter(self.clusterNetworkConfig.routes, x, toLower(x) == 'default'))
            + size(filter(flatten(map(self.podNetworkConfigs, p, p.routes)), x, toLower(x)
            == 'default')) <= 1
    served: true
    storage: true
    subresources:
      status: {}

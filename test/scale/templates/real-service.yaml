apiVersion: v1
kind: Service
metadata:
  name: TEMP_NAME
  namespace: scale-test
  labels:
    app: kapinger
    is-real: "true"
spec:
  selector:
    app: kapinger
    is-real: "true"
    name: TEMP_DEPLOYMENT_NAME
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
